# Memory

## charlie-ai-skills repo conventions

- `SKILL.md` and `cursor.rule.md` are **auto-generated** — never edit them directly.
- Only edit `skill.json`, then run `npm run gen` (or `npm run gen -- path/to/skill.json`) to regenerate.
- This is enforced by the `skill-maintenance` skill.
